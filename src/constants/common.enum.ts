export enum roles {
  admin = 'admin',
  user = 'user',
}

export enum MimeType {
  JSON = 'application/json',
  TEXT = 'text/plain',
}

export enum itemWeightUnit {
  kg = 'kg',
  g = 'g',
  l = 'l',
  ml = 'ml',
  count = 'count',
}

export enum orderSearchKey {
  mobile = 'mobile',
  id = 'id',
  all = 'all',
}
